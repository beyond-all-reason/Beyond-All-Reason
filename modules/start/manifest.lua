---@type ModuleManifestFile
return {
	name = "start",
	description = "Start: a team's start as a region, its positions and the area they sit in, with the match's own startboxes and start positions as the default",
	requires = { "regions" },
}
