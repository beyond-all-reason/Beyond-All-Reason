DrKillinger is used to generate wreckages.

Sometimes a correction of the generated wreck model placement and rotation (bots) is needed via Upspring

requirement: Python 2.7

drkillinger has setup some model/script paths inside so if these change, adjust em!