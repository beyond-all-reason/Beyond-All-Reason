--teams = Spring.GetTeamList()
--metalSpots = GG.metalSpots


function gadget:GameFrame(n)
   -- if n == 30 then
      --  for i = 1,#metalSpots do
        --    local mex = metalSpots[i]
       --     gx = mex.x
      --      gy = mex.y
       --     gz = mex.z
      --      Spring.Echo("Mex "..i..": x = "..gx..", y = "..gy..", z = "..gz..".")
            --Spring.MarkerAddPoint(gx, gy, gz)
     --   end
    --end
end