-- CI fixture. Deliberately broken; delete ci_test/ before merging.
-- 3 misformatted block(s).

local function  chunk_1 ( a,b )
	return  {a,b, 'value' }
end

local function  chunk_2 ( a,b )
	return  {a,b, 'value' }
end

local function  chunk_3 ( a,b )
	return  {a,b, 'value' }
end
